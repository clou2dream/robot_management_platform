# Camel系列仙工控制器 外部接口与协议分析（**完全依据文档内容，无外部延伸**）
## 一、整体结论
该操作手册**明确支持外部接口对接**，覆盖上位机调试、传感器接入、调度系统联动、外设控制、任务远程下发等全场景，所有通信协议与使用场景均来自文档原文。

## 二、文档明确支持的**外部对接接口**
1. Roboshop Pro 上位机接口（有线/无线）
2. RDS/RCS 调度系统接口（Core调度）
3. 第三方激光雷达接口
4. 大华读码相机（PGV）接口
5. 步科驱动器/电机接口
6. 电池管理系统接口
7. 滚筒/顶升/货叉外设接口
8. JSON 任务 API 下发接口
9. Wi‑Fi/以太网远程对接接口

---

## 三、通信协议 + 支持状态 + 使用场景（**严格按文档原文**）
### 1. CANopen
- 支持状态：**完全支持，底盘驱动核心通信协议**
- 使用场景：
  - 步科FD/OD/MD系列伺服驱动器与SRC控制器通信
  - 差速轮、舵轮电机的控制指令下发与状态反馈
  - 驱动器CAN ID配置、250KHz波特率通信
  - 急停信号、电机使能/故障信号传输

### 2. RS485
- 支持状态：**完全支持，传感器/外设通用串口协议**
- 使用场景：
  - 大华PGV读码相机与控制器通信（485A/485B接线）
  - 百维电池数据上报（电量、电压、温度）
  - 多台PGV相机分路通信（485通信3/4接口）
  - 常规外设串口数据传输，默认波特率115200

### 3. TCP/IP（以太网）
- 支持状态：**完全支持，核心网络通信协议**
- 使用场景：
  - Roboshop Pro 有线/无线连接机器人（默认IP：192.168.89.100）
  - 导航/安全激光雷达IP配置与点云数据上传
  - 步科HMI触摸屏烧录与界面通信（IP：192.168.192.20）
  - 大华相机SVStudio上位机连接
  - Wi‑Fi联网、漫游、多车调度网络通信

### 4. Modbus
- 支持状态：**支持，外设控制专用协议**
- 使用场景：
  - 滚筒设备控制与状态反馈
  - 顶升、货叉机构控制（ModbusJack/ModbusTractor）
  - 外部IO扩展模块通信
  - 文档标注：滚筒报错、顶升报错均基于该协议

### 5. HTTP/TCP（RDS调度API）
- 支持状态：**支持，调度系统对接协议**
- 使用场景：
  - RDS Core调度系统添加机器人、下发任务、监控状态
  - 多机协同调度、库位binTask执行
  - 机器人位置、任务进度、故障信息上报调度服务器

### 6. UDP
- 支持状态：**支持，激光雷达数据传输**
- 使用场景：
  - 激光雷达UDP模式点云数据高速上传
  - 局域网传感器数据传输

### 7. DMX512
- 支持状态：**支持，灯光专用协议**
- 使用场景：
  - 机器人LED氛围灯、警示灯控制（WST‑SM16512PS‑DMX512）

