

# 一、核心定位与适用范围
1. **核心作用**
    - 定义**统一、厂商中立**的通信协议，让不同厂家的移动机器人共用一套调度系统。
    - 降低集成复杂度，支持自由导航/循线导航等各类AGV/AMR。
2. **不包含内容**
    - 功能安全标准、交通调度算法、外设通信、项目实施流程、网络安全细则。
3. **版本规则**
    - 大版本（x.0.0）：不兼容改动；小版本（3.x.0）：新增功能；补丁版（3.0.x）：修复勘误。

---

# 二、通信基础架构
1. **传输层**
    - 协议：**MQTT 3.1.1+**
    - 消息格式：**JSON**
2. **MQTT主题结构（强制）**
    `interfaceName/majorVersion/manufacturer/serialNumber/topic`
    示例：`vda5050/v3/KIT/0001/order`
3. **8个核心通信主题**
    | 主题 | 发布方 | 作用 |
    |---|---|---|
    order | 调度 | 下发任务（节点+边+动作） |
    instantActions | 调度 | 立即执行动作（暂停、取消、充电等） |
    state | 机器人 | 上报状态、进度、错误、请求 |
    connection | 机器人/ Broker | 连接状态（在线/离线/断连/休眠） |
    factsheet | 机器人 | 机器人能力参数（尺寸、速度、负载、支持动作） |
    zoneSet | 调度 | 下发区域规则（限速、禁行、协同） |
    visualization | 机器人 | 高频位置与路径（可视化用） |
    responses | 调度 | 响应机器人的区域/走廊请求 |
4. **QoS策略**
    - connection：QoS 1（至少一次）
    - 其余主题：QoS 0（尽力传输）

---

# 三、核心流程：订单(Order)机制
1. **订单结构**
    - 基于**节点（Node）+边（Edge）** 有向图描述路径。
    - 分为：
      - **Base**：已放行，必须执行
      - **Horizon**：未放行，用于前瞻规划
2. **订单更新**
    - 用`orderId + orderUpdateId`唯一标识。
    - 仅追加Base，不修改已下发Base；Horizon可随时修改。
3. **订单取消**
    - 调度下发`cancelOrder`即时动作。
    - 机器人尽快停车，未执行动作置为FAILED。
4. **订单拒绝场景**
    - 格式错误、起点超范围、ID过时、地图不存在、模式不允许接单等。

---

# 四、动作(Actions)体系
1. **三类动作**
    - 节点动作：到达节点执行
    - 边动作：行驶中执行
    - 即时动作：立即执行
2. **4种阻塞类型**
    - NONE：可并行+可行驶
    - SINGLE：不可并行+可行驶
    - SOFT：可并行+禁行驶
    - HARD：不可并行+禁行驶
3. **必选基础动作**
    - cancelOrder、startPause、stopPause
4. **常用标准动作**
    - 充放电、休眠/唤醒、上下料、地图下载/启用、定位初始化、区域触发等。
5. **动作状态**
    WAITING → INITIALIZING → RUNNING → PAUSED → FINISHED/FAILED/RETRIABLE

---

# 五、地图与区域(Zones)管理
1. **地图**
    - 唯一标识：`mapId + mapVersion`
    - 支持：下载→启用→删除流程
    - 坐标：右手坐标系，单位米/弧度
2. **区域（Zone）**
    - 按判断基准分：**轮廓型**、**运动中心型**
    - 典型类型：
      - BLOCKED：禁入
      - RELEASE：需调度授权进入
      - COORDINATED_REPLANNING：需授权重规划
      - SPEED_LIMIT：限速
      - ACTION：区域触发动作
      - LINE_GUIDED：强制循线
3. **区域交互规则**
    - BLOCKED优先级最高；重叠限速取最小值；冲突动作报错。

---

# 六、状态(State)上报机制
1. **上报触发**
    - 事件触发+定时（最少30s）
2. **上报内容**
    - 订单进度、位置/定位、速度、负载、电量、错误、操作模式、动作状态、区域请求、走廊请求。
3. **错误等级**
    - WARNING、URGENT、CRITICAL、FATAL
4. **操作模式**
    - AUTOMATIC（自动）、MANUAL（手动）、SERVICE（服务）、TEACH_IN（示教）等。手动/服务模式会清空订单。

---

# 七、请求-响应(Request/Response)机制
用于需要调度授权的场景：
1. RELEASE区域准入
2. COORDINATED_REPLANNING路径重规划
3. CORRIDOR走廊避障偏移
- 状态：REQUESTED → QUEUED → GRANTED/REJECTED → REVOKED/EXPIRED

---

# 八、 Factsheet（机器人能力表）
机器人主动上报，用于调度自动适配：
- 类型规格（叉取/牵引/ conveying）
- 运动学（差速/全向）
- 物理参数（尺寸、速度、加速度、负载）
- 支持动作、区域、可选参数
- 协议限制（消息长度、数组上限、时序）
- 负载规格、几何轮廓、软硬件版本

---

# 九、关键设计原则
1. **异步可靠**：无线丢包容忍，Base不可改，Horizon可动态调整。
2. **最小耦合**：调度只负责任务与交通，机器人负责定位与避障。
3. **可扩展**：支持自定义动作、区域、参数。
4. **可互操作**：同标准下不同品牌机器人可混跑。

